box: pjvds/golang
build:
  steps:
    - script:
        name: go get
        code: |
          cd $WERCKER_SOURCE_DIR
          go version
          go get github.com/onsi/ginkgo
          go get github.com/onsi/gomega
          go get -t ./...
